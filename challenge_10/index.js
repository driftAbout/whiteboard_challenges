'use strict';

const Stack = require('../lib/stack');

const solution = module.exports = {};

soultion.checkBraces = (braces_str) => {
if (typeof braces_str !== 'string') return new Error('Invalid input: Expecting string');

}
