{
  "name": "challenge_14",
  "version": "1.0.0",
  "description": "Whiteboard Challenge 14 - Write a function called dedupe(head) that accepts the head of a linked list as it's argument, and returns a new linked list - Use a stack to identify any consecutive duplication, and remove them from the list.",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "jest --verbose -i",
    "test:debug": "DEBUG=http* jest --verbose -i"
  },
  "keywords": [
    "linked",
    "list",
    "stacks",
    "remove",
    "consectutive",
    "dupes"
  ],
  "author": "Kevin Miller",
  "license": "MIT",
  "devDependencies": {
    "debug": "^3.1.0",
    "jest": "^22.1.4"
  }
}
