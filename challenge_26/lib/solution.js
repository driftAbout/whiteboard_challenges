'use strict';

const map = require('./utils').map;
const filter = require('./utils').filter;
const reduce = require('./utils').reduce;

const solution = module.exports = {};

solution.map = map;
solution.filter = filter;
solution.reduce = reduce;